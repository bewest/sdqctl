# Example workflow demonstrating RUN, PROLOGUE, EPILOGUE, HEADER, FOOTER
# Run with: sdqctl run examples/workflows/verify-with-run.conv

MODEL gpt-4
ADAPTER mock
MODE audit
MAX-CYCLES 1

# Inject date and context into every prompt
PROLOGUE Analysis started: {{DATE}}
PROLOGUE Working directory: {{CWD}}

# Remind AI to be thorough after every prompt
EPILOGUE Remember to cite specific file locations.

# Run verification tools first
RUN-ON-ERROR continue
RUN-OUTPUT always

# Execute verification tool
RUN python -c "print('Verification result: 5 issues found')"

PROMPT Analyze the verification output above and categorize the issues by severity.

# Run another tool
RUN python -c "print('Coverage: 73%')"

PROMPT Based on the coverage report, identify areas that need more testing.

# Output formatting - use literal description, not {{WORKFLOW_NAME}} (Q-003 best practice)
HEADER # Verification Report
HEADER Generated: {{DATETIME}}

FOOTER ---
FOOTER *This report was generated by sdqctl*

OUTPUT-FORMAT markdown
OUTPUT-FILE reports/verification-{{DATE}}.md
